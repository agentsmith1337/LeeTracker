# PostgreSQL connection string
# Create the database first:  CREATE DATABASE leetrack;
# Local dev example (change password to match your Postgres user)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/leetrack?schema=public"

# NextAuth + mobile JWT signing — generate with: openssl rand -base64 32
AUTH_SECRET="change-me-in-production"